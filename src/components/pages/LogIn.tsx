import React, { FC } from "react";
import Wrapper from "../common/section/Wrapper";

const LogIn: FC = () => {
  return (
    <Wrapper>
      <p>login</p>
    </Wrapper>
  );
};

export default LogIn;
